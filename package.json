{
  "name": "zenn",
  "version": "1.0.0",
  "engines": {
    "bun": ">=1.0.0"
  },
  "scripts": {
    "preinstall": "npx only-allow bun",
    "preview": "zenn preview"
  },
  "devDependencies": {
    "zenn-cli": "^0.5.2"
  }
}
